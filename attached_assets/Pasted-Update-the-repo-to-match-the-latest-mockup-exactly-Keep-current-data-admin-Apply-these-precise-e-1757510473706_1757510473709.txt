Update the repo to match the latest mockup exactly. Keep current data + admin. Apply these precise edits.

1) Fonts (swap usage)

We already have:

/public/fonts/Parslay.ttf

/public/fonts/Billmake-Regular.otf

In public/styles.css, keep the @font-face as is, but swap where they’re used:

/* GLOBAL */
html,body{font-family:'Billmake',system-ui,sans-serif;}   /* body stays Billmake */

/* HEADINGS should now use Billmake too unless overridden */
h1,h2,h3{font-family:'Billmake',sans-serif;color:var(--olive);}

/* HERO LOCKUP — swap fonts between the two lines */
.hero-title .line-1{font-family:'Billmake',sans-serif; font-size:38px; letter-spacing:.02em;}
.hero-title .line-2{font-family:'Parslay',serif; font-size:38px; letter-spacing:.06em;}
@media (max-width:768px){
  .hero-title .line-1,.hero-title .line-2{font-size:30px;}
}

2) Brand colors (use palette + set COMFORT)

Add/ensure these tokens (values from the palette):

:root{
  --cream:#f0eee7;
  --taupe:#cabeac;
  --orange:#efa25f;
  --amber:#efc785;
  --coral:#e46f60;
  --olive:#3b4b30;
  --comfort:#ece6db;                 /* use this for the Allergens band */
  --ink:#26332D;
  --card:#ffffff;
  --radius:14px;
  --shadow:0 8px 24px rgba(0,0,0,.06);
}
html,body{background:var(--cream);color:var(--ink);}

3) Header (no blur, bigger logo, transparent over hero)

Remove any pattern/blur on header. Make logo bigger.

.site-header{
  position:fixed; inset:0 0 auto 0; height:76px; z-index:50;
  background:transparent;            /* no white bar, no blur */
  backdrop-filter:none;              /* remove blur */
  border-bottom:none;
}
.brand img{height:48px;}             /* bigger logo */


Delete any :before/pattern rules applied to header/body. If there’s a global pattern, remove it:

/* remove background pattern if present */
body, .hero, .site-header { background-image:none !important; }

4) Hero (bottom-left lockup + swap fonts)

Update the hero markup to separate the two lines (if not already):

<section class="hero">
  <div class="hero-inner container">
    <div class="hero-lockup">
      <h1 class="hero-title">
        <span class="line-1">The Menu</span><br/>
        <span class="line-2">OF BEYROUTH</span>
      </h1>
    </div>
  </div>
</section>


Keep the existing hero image (/public/images/hero.jpg). Make sure the hero has no overlay icons/pattern and anchors right:

.hero{
  height:520px;
  background:url('/images/hero.jpg') center right/cover no-repeat !important;
  position:relative;
}
.hero-lockup{position:absolute; left:22px; bottom:26px; color:var(--olive);}

5) Allergens band — use COMFORT
.allergens-legend{background:var(--comfort); padding:22px 0; text-align:center; box-shadow:var(--shadow);}

6) Menu cards — white background + same font for title/desc
.menu-card{
  background:#fff;                   /* was var(--card); force white */
  border-radius:var(--radius);
  box-shadow:var(--shadow);
}
.menu-meta h3{                      /* same font as description */
  font-family:'Billmake',sans-serif;
  font-size:18px; color:var(--olive); margin:0 0 6px;
}
.menu-meta p{font-family:'Billmake',sans-serif; color:#7b847e; font-size:13px; margin:0;}

7) Category pills — horizontal scroll + exact colors

Make the categories a scrollable row (mobile-first), with pill colors matching the screenshot.

.menu-tabs{
  display:block; overflow-x:auto; white-space:nowrap; padding:8px 0; margin:18px auto 8px;
  scrollbar-width:thin;
}
.menu-tab{display:inline-flex; align-items:center; gap:8px; margin-right:10px;
  padding:8px 16px; border-radius:999px; border:2px solid transparent; font-weight:700; cursor:pointer;
}

/* exact pill colors */
.menu-tab[data-cat="Breakfast"]{background:var(--olive); color:#fff;}
.menu-tab[data-cat="Appetizers"]{background:var(--coral); color:#fff;}
.menu-tab[data-cat="Main Course"]{background:var(--taupe); color:#fff;}
.menu-tab[data-cat="Plat Du Jour"]{background:var(--orange); color:#fff;}

/* inactive state (when filtered away) */
.menu-tab.is-inactive{background:#fff; border-color:#9aa39d; color:var(--ink);}


If your tabs are rendered as buttons/links, keep the same data-cat attributes. No layout changes to JS logic; only ensure the container has overflow-x:auto.

8) Responsiveness cleanup
@media (max-width:640px){
  .menu-card{grid-template-columns:88px 1fr auto;}
  .menu-thumb{width:88px;height:72px;}
  .head-wrap,.hdr{padding-left:14px;padding-right:14px;}
}

9) Remove any background icon pattern site-wide

Search and delete rules like:

body::before, .hero::before, .pattern, [data-bg="pattern"]{ content:none !important; background:none !important; }

10) No behavior changes

Keep language switcher + Halal badge functionality.

Keep admin + allergens CMS as previously implemented.

Done when

Header is transparent (no blur), bigger logo, sits over hero.

Hero shows bottom-left title with fonts swapped (line1 Billmake, line2 Parslay).

Allergens band uses COMFORT color.

Product cards are white; title and description use the same Billmake font.

Category pills scroll horizontally and use the exact colors above.

No background icons/patterns anywhere.

All remains responsive.