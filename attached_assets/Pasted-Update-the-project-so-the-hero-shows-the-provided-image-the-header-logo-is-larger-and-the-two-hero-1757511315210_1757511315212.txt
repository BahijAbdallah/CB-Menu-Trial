Update the project so the hero shows the provided image, the header logo is larger, and the two hero lines swap fonts.

Do exactly this:

Ensure the hero image path exists

Put the file at: /public/images/hero.jpg (use the image I provided).

If a different hero is referenced anywhere, replace it with /images/hero.jpg.

Edit the hero markup (component that renders the landing header section – e.g., Hero.tsx or in menu.tsx)

Use two spans so we can style each line separately and swap fonts.

<section className="hero" aria-label="Hero">
  <div className="hero-inner container">
    <h1 className="hero-title">
      <span className="line-1">The Menu</span><br />
      <span className="line-2">OF BEYROUTH</span>
    </h1>
  </div>
</section>


CSS — make the background show, remove any overlay/pattern, and position the lockup

In public/styles.css (or the global stylesheet), add/replace these rules:

/* Fonts already loaded; we are SWAPPING which line uses which */
.hero{
  position: relative;
  height: 520px;
  background: url('/images/hero.jpg') center right/cover no-repeat !important;
  /* kill any site-wide pattern/overlay that may hide the image */
}
.hero::before, .hero::after{ content: none !important; background: none !important; }

.hero-inner{ position: relative; height: 100%; }
.hero-title{
  position: absolute; left: 22px; bottom: 28px;
  margin: 0; color: var(--olive);
  line-height: 1.05;
}

/* SWAP FONTS:
   line-1 = Billmake (same as "BEYROUTH" had),
   line-2 = Parslay (same as "The Menu" had) */
.hero-title .line-1{
  font-family: 'Billmake', sans-serif;
  font-size: 38px; letter-spacing: .02em;
}
.hero-title .line-2{
  font-family: 'Parslay', serif;
  font-size: 38px; letter-spacing: .06em;
}

@media (max-width: 768px){
  .hero{ height: 400px; }
  .hero-title .line-1,
  .hero-title .line-2{ font-size: 30px; }
}

/* Header/logo tweaks (no blur, bigger logo) */
.site-header{
  position: fixed; inset: 0 0 auto 0; height: 76px; z-index: 50;
  background: transparent; backdrop-filter: none; border-bottom: none;
}
.brand img{ height: 96px; }                 /* make logo bigger */
@media (max-width: 768px){ .brand img{ height: 72px; } }

/* Make sure nothing else paints over the hero */
body, .site-header, .hero{ background-image: none !important; }


If the image still doesn’t show, force the path from React/Vite:

In the hero component, set an inline style using the public asset:

<section
  className="hero"
  style={{ backgroundImage: "url(/images/hero.jpg)", backgroundPosition: "right center", backgroundSize: "cover", backgroundRepeat: "no-repeat" }}
/>


Remove any conflicting CSS rules that set a different background on .hero.

Keep everything else unchanged (language switcher & Halal badge behavior). Only the hero, header blur removal, bigger logo, and font swap should change.